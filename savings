

            # создать счёт
            Invoke-RestMethod -Uri "http://localhost:8080/accounts" -Method POST -ContentType "application/json" -Body '{"owner_name": "Alex", "balance": 500.00}'

            # пополнить
            Invoke-RestMethod -Uri "http://localhost:8080/accounts/1/deposit" -Method PUT -ContentType "application/json" -Body '{"amount": 100}'

            # снять
            Invoke-RestMethod -Uri "http://localhost:8080/accounts/1/withdraw" -Method PUT -ContentType "application/json" -Body '{"amount": 50}'

            # посмотреть
            Invoke-RestMethod -Uri "http://localhost:8080/accounts/1"

            #перевод от счёта 1 к счёту 4 на сумму 100
            Invoke-RestMethod -Uri "http://localhost:8080/accounts/1/transfer" -Method PUT -ContentType "application/json" -Body '{"toAccountId": 4, "amount": 100}'